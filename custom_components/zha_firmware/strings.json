{
  "config": {
    "step": {
      "user": {
        "title": "ZHA Firmware OTA Manager",
        "description": "Add community firmware sources to ZHA so that devices not covered by the default OTA providers can be updated."
      }
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only a single configuration is possible.",
      "zha_not_configured": "The ZHA integration is not set up. Configure ZHA first, then add this integration."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "OTA sources",
        "description": "Choose which firmware sources to add to ZHA. A firmware update can, in rare cases, break a feature or brick a device.",
        "data": {
          "use_koenkk": "Use the Zigbee2MQTT community index (Koenkk/zigbee-OTA)",
          "use_zigpy": "Use the official zigpy firmware index (zigpy-ota)",
          "extra_urls": "Additional remote index URLs (one per line)",
          "local_folder": "Local firmware folder (optional)",
          "broadcast": "Ask devices to re-check for firmware after changes"
        },
        "data_description": {
          "use_koenkk": "The large community firmware collection used by Zigbee2MQTT; covers hundreds of devices from many manufacturers.",
          "use_zigpy": "A reliable index maintained by the zigpy / Home Assistant developers. Complements the Zigbee2MQTT index with extra device coverage."
        }
      }
    }
  },
  "services": {
    "check_updates": {
      "name": "Check for firmware updates",
      "description": "Re-registers the community OTA sources with ZHA and asks Zigbee devices to re-check for available firmware."
    }
  },
  "entity": {
    "sensor": {
      "ota_sources": {
        "name": "Active OTA sources"
      }
    }
  }
}
