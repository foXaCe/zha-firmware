{
  "config": {
    "step": {
      "user": {
        "title": "ZHA Firmware OTA Manager",
        "description": "Add community firmware sources to ZHA so that devices not covered by the default OTA providers can be updated."
      }
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only a single configuration is possible."
    }
  }
}
